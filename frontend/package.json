{
  "name": "@ddgc/frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "build": "vite build",
    "test": "vitest run",
    "smoke": "vitest run src/validation src/build-run",
    "smoke-browser": "playwright test",
    "smoke-cap": "playwright test --config=playwright-cap.config.ts",
    "smoke-build": "npm run build && vitest run src/validation src/build-run && playwright test && playwright test --config=playwright-cap.config.ts"
  },
  "dependencies": {
    "solid-js": "^1.9.5"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/node": "^22.10.2",
    "jsdom": "^25.0.1",
    "typescript": "^5.7.2",
    "vite": "^5.4.11",
    "vite-plugin-solid": "^2.11.0",
    "vitest": "^2.1.5"
  }
}
